 <style>
    .slider-container {
      position: relative;
      max-width: 1200px;
      margin: auto;
      overflow: hidden;
    }

    .slider-icons {
      display: flex;
      transition: transform 0.3s ease-in-out;
      justify-content:left;
      /* justify-content:center; */
    }

    .slider-icons li {
      list-style: none;
      min-width: 40%; /* 100/4 = 25% for 4 items */
      /* box-sizing: border-box; */
      padding: 5px 5px;
      /* background: white; */
      margin: 5px;
      text-align: center;
      /* border-radius: 4px; */
      /* box-shadow: 0 2px 5px rgba(0,0,0,0.1); */
    }

    .arrow {
      position: absolute;
      top: 50%;
      transform: translateY(-50%);
      background-color: transparent;
      color: black;
      border: none;
      padding: 10px 15px;
      cursor: pointer;
      z-index: 1;
      border-radius: 50%;
    }

    .arrow.left {
      left: 0px;
    }

    .arrow.right {
      right: 0px;
    }

    .top-notes-style{
      text-align:center; 
      margin-top:20px;
    }
   .slider-icons li img{
     width: 50%;
    height: auto;
    object-fit: cover;
    /* display: block; */
    /* border-radius: 8px; */
   }

    @media (max-width: 768px) {
      .slider-icons li {
        min-width: 30%;
      }
      .slider-icons {
        margin-left:0px;
        margin-right:0px;
      }
      
    }

  </style>